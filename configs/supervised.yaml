settings:
  checkpoint_path: "weights/supervised"
  log_dir: "loggers/runs/supervised"
  
  model_name: "efficientnet-b0"
  device: "cuda:0"

  classes: [
    "airplane", 
    "automobile", 
    "bird", 
    "cat", 
    "deer", 
    "dog",
    "frog" ,
    "horse", 
    "ship",
    "truck"]
    
  num_epochs: 50
  print_per_iter: 10

  img_size: 32
  batch_size: 128